services:
  - type: web
    name: paste
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: uploads
      mountPath: /opt/render/project/src/uploads
      sizeGB: 1
    envVars:
      - key: NODE_VERSION
        value: 18.17.1
